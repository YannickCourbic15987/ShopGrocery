{% extends 'base.html.twig' %}

{% block title %}Nos Produits{% endblock %}

{% block content %}

<div class="container d-flex ">
<h3 class="text-capitalize mb-5 mr-5">Nos produits </h3>
<h3 > Filtrer </h3>

</div>
    <div class="row">
{% for product in products  %}
    <div class="col-md-4 mb-5  mx-auto">
    {# {{ dump(product) }} #}
    <div class="product-item text-center">
     <h6 class="text-uppercase fw-ligther mb-2">
       
       {{product.name}}
     </h6>
      {# image ici , feature en cours #}
     <a href="{{path('product' , {'slug' : product.slug})}}">  <img src="../../public/uploads/{{product.illustration}}" alt="{{product.name}}" class="img-fluid rounded-circle border border-dark border-2" > </a>
     <span class ="d-block">
     {{product.subtitle}}
     </span>
       <span>
       {{(product.price /100)| number_format(2 , ',' , '.')}} €
       </span>
    </div>    
    </div>
    
{% endfor %}
    
    </div>

{% endblock %}
